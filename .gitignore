# Editor/IDE
.vscode/
.idea/

# Env
.env
.env.*

# Test/runtime artifacts
.tmp_*
/tmp/

# Backup/snapshot dirs
.dotfiles-backup-*

# Secrets cache
.secrets.json
.secrets/*
secrets/secrets.yaml
secrets/secrets.yaml.*

# macOS metadata
.DS_Store
.AppleDouble
.LSOverride

# Windows metadata
Thumbs.db
desktop.ini

# Nix/GC roots
result

# Logs
*.log

# Local-only working notes. These routinely contain real identity data
# (email, GPG key ids, absolute $HOME paths) and machine-specific findings,
# which must never be published from this repo. Keep them local.
docs/
reviews/
