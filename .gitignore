# Protect sensitive information - never commit personal data
*.local
*.private
*_local
*_private
bootstrap-private.sh

# Personal directories (do not track)
personal/
private/

# Object and binary files
*.o
*.out

# XDG cache directory (runtime logs, prompt caches)
.cache/

# Age / SOPS secret management
# secrets.yaml      (decrypted working copy — gitignored below)
*.age
*.age-key
*.key.enc

# Decrypted working copy of secrets (both legacy and new locations)
secrets.yaml
secrets/secrets.yaml

# Private age key (lives outside the repo at ~/.config/sops/age/keys.txt)
