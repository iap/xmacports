name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  XDG_CONFIG_HOME: ${{ runner.temp }}/.config
  XDG_CACHE_HOME: ${{ runner.temp }}/.cache
  XDG_DATA_HOME: ${{ runner.temp }}/.local/share
  XDG_STATE_HOME: ${{ runner.temp }}/.local/state
  DEBIAN_FRONTEND: noninteractive

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install mise
        uses: jdx/mise-action@v2
        with:
          install: true

      - name: Install system packages (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3 python3-yaml git curl ca-certificates

      - name: Generate age keypair (for secret tests)
        run: |
          mkdir -p "$XDG_CONFIG_HOME/sops/age"
          age-keygen -o "$XDG_CONFIG_HOME/sops/age/keys.txt"

      - name: Create XDG runtime directories
        run: |
          mkdir -p "$XDG_DATA_HOME" "$XDG_STATE_HOME/shell" "$XDG_STATE_HOME/bash" "$XDG_STATE_HOME/zsh" "$XDG_CACHE_HOME/shell" "$XDG_CACHE_HOME/logs" "$XDG_CACHE_HOME/ssh"

      - name: Run test suite
        run: |
          set -euo pipefail
          make test
          make verify
          make shellcheck
          make fmt-check
          make test-secrets
          make test-compliance